class Solution {
public:
    void reverseString(vector<char>& s) {
        int len = s.size()-1;
        for(int i=0;i<=len;i++)
        {
           char t=s[i];
            s[i]=s[len];
            s[len] = t;
            len--;
        }
    }
};
